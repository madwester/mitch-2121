import React from 'react';
import Image from '../images/nz2.png';
import styled from "@emotion/styled";
import Hero from '../components/Hero';

const Home = () => {
  return(
    <div>
      <Hero image={Image}/>
    </div>
  );
};

export default Home;